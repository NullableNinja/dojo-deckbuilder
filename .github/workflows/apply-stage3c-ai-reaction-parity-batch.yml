name: Apply Stage 3C AI Reaction Parity Batch Once

on:
  push:
    branches:
      - stage3c-play-surface-remediation

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply AI Reaction parity batch
        run: |
          python3 scripts/_stage3c_ai_reaction_parity_batch.py
          git diff --check
          node --test tests/stage3c-consumable-reaction-ai.test.mjs tests/playtest-effect-integration.test.mjs
      - name: Commit product changes
        run: |
          rm scripts/_stage3c_ai_reaction_parity_batch.py
          rm .github/workflows/apply-stage3c-ai-reaction-parity-batch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/playtest.tsx tests/playtest-effect-integration.test.mjs scripts/_stage3c_ai_reaction_parity_batch.py .github/workflows/apply-stage3c-ai-reaction-parity-batch.yml
          git commit -m "fix: enforce AI defensive Consumable Reaction parity"
          git push origin HEAD:stage3c-play-surface-remediation
