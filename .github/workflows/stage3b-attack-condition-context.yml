name: Apply Stage 3B Attack condition context

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/stage3b-attack-condition-context.yml"

permissions:
  contents: write
  actions: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply strict source patch
        run: node scripts/patch-attack-condition-context.mjs

      - name: Commit patched source and remove one-time migration helpers
        run: |
          rm .github/workflows/stage3b-attack-condition-context.yml
          rm scripts/patch-attack-condition-context.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "engine: expose structured Attack condition context"
          git push origin HEAD:main
