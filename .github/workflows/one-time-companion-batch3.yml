name: One-time companion batch three

on:
  push:
    branches: [site-polish-batch3]
    paths:
      - .github/workflows/one-time-companion-batch3.yml

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: site-polish-batch3
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply deep-link patch
        run: python scripts/one_time_companion_batch3.py
      - name: Install dependencies
        run: npm ci
      - name: Run complete test suite
        run: npm test
      - name: Remove temporary patch files
        run: rm scripts/one_time_companion_batch3.py .github/workflows/one-time-companion-batch3.yml
      - name: Commit validated batch
        run: |
          git config user.name "NullableNinja Site Automation"
          git config user.email "130773741+NullableNinja@users.noreply.github.com"
          git add -A
          git commit -m "Add shareable companion deep links and rule section navigation"
          git push origin HEAD:site-polish-batch3
